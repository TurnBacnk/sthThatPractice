package LeetCodePrograming;

public class Solution {
    public static boolean isPalindrome(int x) {
        if(x<0){
            return false;
        }
        Integer num = x;
        int length = num.toString().length();
        for (int i = 1; i < length; i++) {
            int num1 = (int)Math.pow(10,length-i);
        }

        x/
    }

}

